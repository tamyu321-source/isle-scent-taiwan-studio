# PURE WHITE 與作品集驗收

2026-09-13，本機開發版與 GitHub Pages 子路徑靜態產物。

## 作品集

- 作品區無可見標題、說明或快速索引，保留 `aria-label="作品集"`。
- 顯示編號為 01–06；識別依序仍是 `project-01`、`project-05`、`project-02`、`project-03`、`project-04`、`project-06`。
- 1440px 下前兩件全寬；03/04 與 05/06 各同列。390px 和 320px 下單欄，順序正確且無水平溢出。
- 優格預覽為實際 1440 × 900 首頁截圖，轉成 WebP，非合成介面圖。

## 優格互動與容錯

- 首頁選擇 450g 後進入產品頁，規格為分享罐；產品頁切換規格會更新 query，重新整理可還原。
- 規格可用方向鍵移動焦點、空白鍵選取。日常搭配分頁與 FAQ 可切換。
- 手機選單展開、關閉與區塊連結正常；跳往製程後焦點落在製程區，選單已移除。
- 返回品牌首頁及作品集連結在子路徑下正常。
- 桌面 1440 × 900 及手機 390 × 844 檢查三段產品分鏡，快速捲動、反向捲動與視窗尺寸切換後仍依閱讀位置顯示。
- 製程依閱讀位置標記鮮乳、發酵、過濾，桌面影像保持在視窗內。
- 手機首頁質地區的文案與影像改為上下排列，避免產品遮住文案。
- 開啟 reduced motion 後，三個分鏡皆為不透明、正常文件排列，沒有固定長捲動區。
- 同時阻擋產品圖片並停用 JavaScript 後，三段文字仍可閱讀；每段保留版面高度，無水平溢出。測試後已恢復瀏覽器設定。

## 靜態建置

- TypeScript 檢查與 `git diff --check` 通過。
- 使用 `GITHUB_ACTIONS=true`、`GITHUB_REPOSITORY=tamyu321-source/isle-scent-taiwan-studio` 完成建置，預先產生 28 條路由（含 404）。
- 路由準備腳本依 HTML 建立入口；`pure-white/original/index.html`、`shareflow/index.html` 及 `shareflow/admin/index.html` 存在。
- 檢查 54 份 HTML（含原始檔和目錄入口）的 1,661 個本機連結及資源引用，沒有缺檔。
- 作品集、六個作品前台、優格產品詳情與相關後台／會員共 13 個入口，透過本機子路徑靜態伺服器皆回應 HTTP 200。
- 瀏覽器直接載入產品深層路由後可操作規格，圖片及腳本使用正確子路徑，重新整理可用。

## 發布範圍

只提交本次作品集、優格、影像與 Pages 路由準備修改。`app/portfolio-about.css`、`components/portfolio-about.tsx` 的既有未提交修改不納入。GitHub `main` 透過既有 Actions 發布；Sites 不同步。
