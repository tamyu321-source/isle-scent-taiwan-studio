# FIELDWORK and PURE WHITE — acceptance record

## Real data, 2026-09-13 (Taiwan time)

- Official Restaurant ZIP downloaded successfully: 589 Tainan and 314 Taoyuan records, 903 total. These are source-listed records, not a census of businesses.
- Initial live run parsed 10 Tainan HTML pages. A second bounded run parsed 100 further pages successfully, giving 110 retained page observations. Both runs reported `ok`.
- Official RestaurantID joined business and service-time lists. The HTML adapter read the public `__NEXT_DATA__` payload and verified each page ID before applying observations.
- Source URLs, source timestamps, successful fetch timestamps and separate attempt/error states are retained. No business was claimed to have been contacted or confirmed open.
- Sources: https://data.gov.tw/dataset/7779 and https://www.twtainan.net/zh-tw/gwoia/ . No third-party descriptions, photos, reviews or Google ratings copied into the dataset.

## Automated checks

- 14 Python standard-library tests: HTML parsing/identity mismatch, service-time join, missing values, branch preservation, duplicate IDs, conflicting observations, failed source fallback, no-snapshot failure, robots 403/404/disallow, bounded timeout retries, URL restrictions, CSV Unicode/quotes/newlines/formula safety, and complete checkpoints despite filtered output.
- 5 Node tests: real-seed schema, composed filtering, sorting without mutation, filtered export content, unsafe CSV prefixes and source URLs.
- `tsc --noEmit` passed. The downloadable ZIP was extracted into a separate QA directory; its CLI help and all 14 packaged tests passed without installing dependencies.
- Static export with `GITHUB_ACTIONS=true` and `GITHUB_REPOSITORY=tamyu321-source/isle-scent-taiwan-studio` produced all 29 output routes. Nested directory entrances are generated by the existing Pages preparation script.

## Browser acceptance

Tested the static Pages build under `/isle-scent-taiwan-studio/`, not just the development server.

- Desktop 1366/1440px and mobile 390px: search, city selection, pagination and checkbox selection. Taoyuan returned 314 records; 和興 returned the expected one record and two source observations.
- CSV and JSON export actions produced one record matching the visible filter. Runtime Blob bytes confirmed UTF-8 BOM `EF BB BF`; JSON included both source observations and export count. The in-app browser did not emit its download-event notification, so content was inspected at Blob creation, without changing the export implementation. The temporary inspection hook was restored.
- Selection remained available when filtering to zero results; filtered export disabled at zero, selected export stayed enabled for 15 selected rows.
- Desktop and mobile detail panels displayed source facts, URLs and dates. Escape returned keyboard focus to the invoking detail button.
- Simulated snapshot-fetch failure retained the previous records and displayed a clear error. The fetch override was restored after testing.
- Optional WebMCP read tool registered with a read-only annotation, returned current data, and rejected an invalid `limit: 0` without changing the UI.
- PURE WHITE: all three recipe tabs changed the recipe, amounts and image; keyboard ArrowRight switched from afternoon to savory. Both homepage and product page use the shared component.
- Product detail retained `?size=jar`; mobile navigation and reduced-motion mode worked. Images blocked in the browser still left full recipe text readable with no horizontal overflow. Network blocking, cache and media overrides were removed after the test.

Screenshots are retained locally in `outputs/fieldwork-qa/`: desktop, mobile, recipe-desktop and recipe-mobile. The portfolio thumbnail is a real browser screenshot, not an interface illustration. Food imagery and recipe specifications are documented in `docs/pure-white-recipes.md`.

## Deployment boundaries

Only GitHub `main` and its existing Pages workflow are used. The workflow adds daily 00:17 UTC collection, manual dispatch, tests, a run summary, Python ZIP packaging and static deployment. Data updates go into deployment artifacts rather than daily commits. Initial checked-in data is a real successful crawl.

The workflow may retain an older valid snapshot and report `stale` or `partial` when a source fails. Scheduled GitHub jobs can be delayed or disabled; no exact-time SLA is claimed. Live deployment and workflow completion are verified after pushing.

The pre-existing changes in `app/portfolio-about.css` and `components/portfolio-about.tsx` are excluded from the commit.
