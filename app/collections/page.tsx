import type { Metadata } from "next";
import { CollectionsPage } from "@/components/brand-pages";

export const metadata: Metadata = { title: "香氣系列" };
export const dynamic = "force-static";

export default CollectionsPage;
