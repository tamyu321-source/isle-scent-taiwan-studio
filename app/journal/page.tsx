import type { Metadata } from "next";
import { JournalPage } from "@/components/brand-pages";

export const metadata: Metadata = { title: "誌記" };
export const dynamic = "force-static";

export default JournalPage;
