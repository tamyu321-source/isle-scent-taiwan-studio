import type { Metadata } from "next";
import { ArticlePage } from "@/components/brand-pages";

export const metadata: Metadata = { title: "沿著立霧溪，記錄石頭的溫度" };
export const dynamic = "force-static";

export default ArticlePage;
