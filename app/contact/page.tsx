import type { Metadata } from "next";
import { ContactPage } from "@/components/brand-pages";

export const metadata: Metadata = { title: "聯絡合作" };
export const dynamic = "force-static";

export default ContactPage;
