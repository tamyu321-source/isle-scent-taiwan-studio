import type { Metadata } from "next";
import { StockistsPage } from "@/components/brand-pages";

export const metadata: Metadata = { title: "體驗據點" };
export const dynamic = "force-static";

export default StockistsPage;
