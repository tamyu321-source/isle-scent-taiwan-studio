import type { Metadata } from "next";
import { SpacesPage } from "@/components/brand-pages";

export const metadata: Metadata = { title: "空間香氛" };
export const dynamic = "force-static";

export default SpacesPage;
