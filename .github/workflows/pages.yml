name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: '17 0 * * *'

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    timeout-minutes: 30
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Test Fieldwork adapters and exports
        run: |
          python -X utf8 -m unittest discover -s tools/fieldwork -p 'test_*.py' -v
          node --test tests/fieldwork.test.mjs
      - name: Test VECTOR project integrity
        run: node --experimental-strip-types --test tests/vector.test.mjs
      - name: Test ClassNest booking and credit transactions
        run: node --experimental-strip-types --test tests/classnest.test.mjs
      - name: Install Checkpoint browser
        run: |
          python -m pip install -r tools/checkpoint/requirements.txt
          python -m playwright install --with-deps chromium
      - name: Test Checkpoint failure handling
        run: |
          python -X utf8 -m unittest discover -s tools/checkpoint -p 'test_*.py' -v
          node --test tests/checkpoint.test.mjs
      - name: Refresh public merchant records
        run: >-
          python -X utf8 tools/fieldwork/fieldwork.py
          --output-dir public/data/fieldwork
          --seed public/data/fieldwork/merchants.json
          --snapshot-url https://tamyu321-source.github.io/isle-scent-taiwan-studio/data/fieldwork/merchants.json
          --max-pages 100
      - name: Report collection status
        run: node scripts/fieldwork-summary.mjs
      - name: Package Python download
        run: |
          node scripts/package-fieldwork.mjs
          node scripts/package-checkpoint.mjs
      - name: Build static site
        run: npm run build
      - name: Prepare clean URLs
        run: node scripts/prepare-github-pages.mjs
      - name: Checkpoint browser release gate
        id: checkpoint
        run: node scripts/run-checkpoint.mjs
      - name: Preserve failed browser evidence
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: checkpoint-failure-${{ github.run_id }}
          path: outputs/checkpoint-ci/
          retention-days: 7
          if-no-files-found: ignore
      - name: Configure Pages
        uses: actions/configure-pages@v5
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist/client
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
